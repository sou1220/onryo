<%= form_with url: tag_search_path, method: :get, local: true do |f| %>
  <% tag = Post.select(:tag).distinct %>
  <%= f.collection_select(:word, tag, :tag, :tag) %>
  <%= f.submit "タグを検索", class: "btn btn-primary" %>
<% end %>

  