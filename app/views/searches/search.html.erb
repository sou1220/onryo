<% if @model == "User" %>
  <h3>Users search for "<%= @word %>"</h3>
  <table class = "table">
    <thead>
     <tr>
       <th>
       </th>
       <th scope = "col">
         名前
       </th>
       <th scope= "col">
         施設名
       </th>
       <th scope = "col">
         コメント
       </th>
     </tr>
    </thead>
    
    <% @users.each do |user| %>
  <tbody>
  
    <% if user.posts.any? %>
        <tr>
          <td>
            <%= image_tag user.posts.first.image %>
          </td>
          <td>
            <%= user.name %>
          </td>
          <td>
            <%= user.posts.first.title %>
          </td>
          <td>
            <%= user.posts.first.body %>
          </td>
        </tr>
        <% end %>
      <% end %>

    </tbody>
  </table>
<% elsif @model == "Post" %>
  <h3>Posts search for "<%= @word %>"</h3>
    <table class = "table">
      <thead>
       <tr>
         <th>
         </th>
         <th scope = "col">
           名前
         </th>
         <th scope= "col">
           施設名
         </th>
         <th scope = "col">
           コメント
         </th>
       </tr>
      </thead>
    <tbody>
      <% @posts.each do |post| %>
        <tr>
          <td>
            <%= image_tag post.get_image %></br>
          </td>
          <td>
            <%= post.user.name %>
          </td>
          <td>
            <%= post.title %>
          </td>
          <td>
            <%= post.body %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
  